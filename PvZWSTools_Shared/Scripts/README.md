这里的 .py **不走 `配置文件/` 那套文件分发**，而是编译进程序集（projitems 里是 `<EmbeddedResource>`），
运行时由 `Helpers/EmbeddedScript.cs` 按名字取出来发给游戏。

用得上这种方式的只有一类脚本：内容必须和宿主代码同步演进的（花园那一份要按格子坐标
现拼占位符，缺文件就等于花园页整个不能用，而它又不该被用户改）。
其余脚本一律放 `配置文件/控件/<分类>/`，随 need.zip 分发、用户可自己改。
