<Window
    x:Class="PvZWSTools_WPF.Views.ExitConfirmDialog"
    Background="{DynamicResource BgRootBrush}"
    FontFamily="Microsoft YaHei UI"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:loc="clr-namespace:PvZWSTools_WPF.Themes"
    Title="{loc:Loc '退出应用'}"
    Width="380"
    SizeToContent="Height"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,10"
            FontSize="14"
            Text="{loc:Loc '确定要退出吗？当前设置已自动保存。'}"
            TextWrapping="Wrap" />

        <CheckBox
            x:Name="DontAskCheckBox"
            Grid.Row="1"
            Margin="0,5"
            Content="{loc:Loc '不再弹出退出提示'}"
            FontSize="13" />

        <StackPanel
            Grid.Row="2"
            Margin="0,10,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Width="70"
                Height="28"
                Margin="0,0,10,0"
                Click="OkButton_Click"
                Content="{loc:Loc '确定'}" />
            <Button
                x:Name="CancelButton"
                Width="70"
                Height="28"
                Click="CancelButton_Click"
                Content="{loc:Loc '取消'}"
                IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
