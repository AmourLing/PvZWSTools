<Window
    x:Class="PvZWSTools_WPF.Views.PasswordDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="程序已过期"
    Width="350"
    Height="220"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,10"
            FontSize="14"
            Text="程序已超过使用期限，请输入密码继续使用"
            TextWrapping="Wrap" />

        <TextBlock
            Grid.Row="1"
            Margin="0,5"
            Text="密码：" />
        <PasswordBox
            x:Name="PasswordBox"
            Grid.Row="2"
            Height="26"
            Margin="0,5,0,10"
            FontSize="14"
            PasswordChar="*" />

        <TextBlock
            x:Name="ErrorText"
            Grid.Row="3"
            Margin="0,5"
            FontSize="12"
            Foreground="Red"
            Text="" />

        <StackPanel
            Grid.Row="4"
            Margin="0,10,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Width="70"
                Height="28"
                Margin="0,0,10,0"
                Click="OkButton_Click"
                Content="确定"
                IsDefault="True" />
            <Button
                Width="70"
                Height="28"
                Click="CancelButton_Click"
                Content="取消"
                IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
